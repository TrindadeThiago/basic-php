<?php
    // Operadores de comparação
    // == igual
    // === idêntico
    // != ou <> diferente
    // !== não idêntico
    // < menor que
    // > maior que
    // <= menor ou igual
    // >= maior ou igual

    // Operadores lógicos
    // && ou AND = todas expressões verdadeira
    // || ou OU = uma das expressões verdadeira
    // XOR = apenas se uma das expressões forem verdadeiras
    // ! = inverte os resultados

    if(!(5 < 4)){
        echo 'verdadeiro';
    } else{
        echo 'falso';
    }
?>